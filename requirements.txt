# Pinned to the versions this codebase is verified green against.
#
# pandas is held below 3.0 DELIBERATELY. pandas 3 makes copy-on-write mandatory,
# so DataFrame.to_numpy() returns a READ-ONLY array. The feature derivation in
# src/features/derive.py assigns into those arrays in place (e.g.
# `interval[n_gaps < 1] = np.nan`), which raises ValueError under pandas 3 and
# fails 57 tests. Do not relax this pin without reworking derive.py and
# re-verifying every published number.
#
# Regenerate with:  .venv\Scripts\python.exe -m pip freeze
cloudpickle==3.1.2
contourpy==1.3.3
cycler==0.12.1
fonttools==4.64.0
imbalanced-learn==0.14.2
itsdangerous==2.2.0
joblib==1.6.0
kiwisolver==1.5.1
matplotlib==3.11.1
narwhals==2.25.0
numpy==2.5.2
packaging==26.3
pandas==2.3.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
PyYAML==6.0.3
scikit-learn==1.9.0
scipy==1.18.1
seaborn==0.13.2
six==1.17.0
sklearn-compat==0.1.6
threadpoolctl==3.6.0
tzdata==2026.3
xgboost==3.4.1

# ---- the product web layer (src/api) -------------------------------------
# Starlette + Jinja2 only. No FastAPI, no ORM, no auth framework — and
# deliberately NO outbound HTTP client: tests/test_containment_outbound.py
# fails the build if one ever becomes importable from the product tree.
starlette==1.6.0
uvicorn==0.52.4
jinja2==3.1.6
python-multipart==0.0.32
